# Structured projects

The experimental plan subsystem was retired. Migration `0008_drop_plan_task.sql`
removes its last schema field; current work organization uses the
[Spaces → Groups → Sessions model](loom-ui.md#app-shell-and-workbench).
