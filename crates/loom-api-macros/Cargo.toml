[package]
name = "loom-api-macros"
version = "0.1.0"
edition = "2021"
description = "Derives that project one operation declaration onto REST, CLI, and MCP."

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "extra-traits"] }
